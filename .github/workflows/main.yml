name: Tests

on: pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: echo hello world

  unit_tests:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: echo hello world

  ui_tests:
    needs: unit_tests
    runs-on: ubuntu-latest
    steps:
    - name: build
      run: echo hello world
